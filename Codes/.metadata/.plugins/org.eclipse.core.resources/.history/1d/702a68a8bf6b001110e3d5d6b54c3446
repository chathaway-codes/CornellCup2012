#include <QtCore>
#include <QCoreApplication>

#include "IO_Thread.hpp"


int main(int argc, char *argv[])
{
    QCoreApplication a(argc, argv);

    IO_Thread::getInstance()->log(INFO, "MAIN", "This is a test of the message logging system");
	
    return a.exec();
}
